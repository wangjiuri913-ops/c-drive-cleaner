# 使用说明

## 启动和扫描

完整解压运行包，双击 `CDriveCleaner.exe`。默认仅处理修改时间超过 3 天的文件。先点击“扫描”，确认每一项来源和路径后，再决定是否清理。

推荐项包含用户临时文件、DirectX 缓存、Windows 错误报告、微信日志/崩溃信息和浏览器缓存。不存在的目录会显示“不存在”。浏览器配置、书签、密码和微信聊天记录不作为本程序的清理目标。

## 执行清理

先关闭相关应用，再点击“清理所选”。确认后永久删除所选缓存和日志，不经过回收站。正在使用、无权限或无法读取的文件会被跳过。

调整保留天数后必须重新扫描。双击表格中的条目可以在资源管理器中定位目录。

## Windows 系统工具

- 打开存储设置：使用 Windows 自带存储管理页面继续查看系统临时文件。
- 清空回收站：仅清空 C 盘回收站，操作不可通过回收站撤销。
- 清理组件存储：调用 `DISM /Online /Cleanup-Image /StartComponentCleanup`，请求管理员权限，不手工删除 WinSxS。
- 关闭休眠：调用 `powercfg /hibernate off`，释放休眠文件，同时失去休眠和快速启动功能。
- 恢复休眠：调用 `powercfg /hibernate on`，重新创建休眠文件并恢复相关能力，实际可用功能取决于 Windows 设置。

系统命令由 Windows 执行。程序记录“已请求/已启动”不等于系统命令成功完成；请查看管理员窗口结果，完成后重新打开程序刷新空间和休眠状态。

## 不要手工清理的目录

不要整目录删除 `Windows\System32`、`Windows\WinSxS`、`Windows\Installer`、`Program Files` 或 `AppData`。不要根据扩展名批量删除 `.dll`、`.sys`、`.exe`，也不要为了腾空间直接删除虚拟内存文件。

微信聊天媒体文件请在微信内部的存储管理中核对并备份后处理。不认识的微信插件或程序目录，不建议手工删除。

## 数据与网络

程序在本机运行，没有上传日志或云端存储功能。操作日志显示在窗口中，不需要登录账号。
